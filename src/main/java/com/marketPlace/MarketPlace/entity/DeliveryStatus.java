// ─── DeliveryStatus Enum ──────────────────────────────────────────────────────

package com.marketPlace.MarketPlace.entity;

public enum DeliveryStatus {
    REQUESTED,
    CONFIRMED,
    PROCESSING,
    SHIPPED,
    OUT_FOR_DELIVERY,
    DELIVERED,
    FAILED,
    CANCELLED
}